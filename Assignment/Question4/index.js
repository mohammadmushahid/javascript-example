/* 4.Create a program that calculates and displays the area of a rectangle. Prompt the user
for the length and width of the rectangle, store the values in variables, and use them to
calculate the area.
 */

const value_One = prompt("Enter Length");
const value_Two = prompt("Enter Width");
const calculate = value_One * value_Two;
console.log(calculate);